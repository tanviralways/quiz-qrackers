import React from "react";

const Blog = () => {
  return (
    <div>
      <div className="mx-20">
        <div className="bg-orange-200 mt-10 px-16 ">
          <h1 className="text-2xl">1.Purposer of react router?</h1>
          <p>
            Ans:React Router is a standard library for routing in React. It
            enables the navigation among views of various components in a React
            Application, allows changing the browser URL, and keeps the UI in
            sync with the URL.
          </p>
        </div>
        <div className="bg-orange-200 mt-10 px-16">
          <h1 className="text-2xl">2.How does context api work?</h1>
          <p>
            Ans:React Router is a standard library for routing in React. It
            enables the navigation among views of various components in a React
            Application, allows changing the browser URL, and keeps the UI in
            sync with the URL.
          </p>
        </div>
        <div className="bg-orange-200 mt-10 px-16">
          <h1 className="text-2xl">3.What is useRef hook in react?</h1>
          <p>
            Ans:The useRef Hook allows you to persist values between renders. It
            can be used to store a mutable value that does not cause a re-render
            when updated. It can be used to access a DOM element directly.
          </p>
        </div>
      </div>
    </div>
  );
};

export default Blog;
